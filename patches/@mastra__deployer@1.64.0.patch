diff --git a/dist/analyze-IM-jyN5x.cjs b/dist/analyze-IM-jyN5x.cjs
index 7f4a32d0a03cb1e2ceeb0eb20de29f75dba2df02..6e93ca85235c69a3659dfa51f5fabc4cf9f395ff 100644
--- a/dist/analyze-IM-jyN5x.cjs
+++ b/dist/analyze-IM-jyN5x.cjs
@@ -936,7 +936,9 @@ function checkConfigExport(result) {
 			const decl = path.node.declaration;
 			if (_babel_core.types.isVariableDeclaration(decl)) {
 				const varDecl = decl.declarations[0];
-				if (_babel_core.types.isIdentifier(varDecl?.id, { name: "mastra" }) && _babel_core.types.isNewExpression(varDecl.init) && _babel_core.types.isIdentifier(varDecl.init.callee, { name: "Mastra" })) result.hasValidConfig = true;
+				const isDirectMastra = _babel_core.types.isNewExpression(varDecl?.init) && _babel_core.types.isIdentifier(varDecl.init.callee, { name: "Mastra" });
+				const isFsAgentWrapper = _babel_core.types.isIdentifier(varDecl?.init, { name: "__mastra" });
+				if (_babel_core.types.isIdentifier(varDecl?.id, { name: "mastra" }) && (isDirectMastra || isFsAgentWrapper)) result.hasValidConfig = true;
 			}
 			/**
 			* 2) export { foo as mastra }
diff --git a/dist/analyze-z37DoML5.js b/dist/analyze-z37DoML5.js
index e4c68aaad21c0232dcb5fead1f61ea537859c82c..65cf1152e14ee8bc90d324fd6cdb59ec04101a4a 100644
--- a/dist/analyze-z37DoML5.js
+++ b/dist/analyze-z37DoML5.js
@@ -948,7 +948,9 @@ function checkConfigExport(result) {
 			const decl = path.node.declaration;
 			if (types.isVariableDeclaration(decl)) {
 				const varDecl = decl.declarations[0];
-				if (types.isIdentifier(varDecl?.id, { name: "mastra" }) && types.isNewExpression(varDecl.init) && types.isIdentifier(varDecl.init.callee, { name: "Mastra" })) result.hasValidConfig = true;
+				const isDirectMastra = types.isNewExpression(varDecl?.init) && types.isIdentifier(varDecl.init.callee, { name: "Mastra" });
+				const isFsAgentWrapper = types.isIdentifier(varDecl?.init, { name: "__mastra" });
+				if (types.isIdentifier(varDecl?.id, { name: "mastra" }) && (isDirectMastra || isFsAgentWrapper)) result.hasValidConfig = true;
 			}
 			/**
 			* 2) export { foo as mastra }
diff --git a/dist/services-B9KRKBT-.js b/dist/services-B9KRKBT-.js
index 98863f5f51424b5541814f6a7c47663c57b0eab5..0c4841d85f60bfe7e61062aded55d0394350c147 100644
--- a/dist/services-B9KRKBT-.js
+++ b/dist/services-B9KRKBT-.js
@@ -167,9 +167,60 @@ function copyPnpmWorkspaceSettings(source, options = {}) {
 		blocks.push(architectureBlock.join("\n"));
 	}
 	if (options.pnpmOverrides && Object.keys(options.pnpmOverrides).length > 0) blocks.push(["overrides:", ...Object.entries(options.pnpmOverrides).map(([key, value]) => `  ${JSON.stringify(key)}: ${JSON.stringify(value)}`)].join("\n"));
+	if (options.patchedDependencies && Object.keys(options.patchedDependencies).length > 0) {
+		blocks.push(["patchedDependencies:", ...Object.entries(options.patchedDependencies).map(([key, value]) => `  ${JSON.stringify(key)}: ${JSON.stringify(value)}`)].join("\n"));
+		blocks.push("allowUnusedPatches: true");
+	}
 	if (options.pnpmNodeLinker) blocks.push(`nodeLinker: ${options.pnpmNodeLinker}`);
 	return ["packages:\n  - '.'", ...blocks].join("\n\n") + "\n";
 }
+function readPnpmPatchedDependencies(source) {
+	if (!source.trim()) return {};
+	let config;
+	try {
+		config = parse(source);
+	} catch (error) {
+		throw new MastraError({
+			id: "DEPLOYER_INVALID_PNPM_PATCH_CONFIG",
+			domain: ErrorDomain.DEPLOYER,
+			category: ErrorCategory.USER,
+			text: "Invalid pnpm patchedDependencies configuration"
+		}, error);
+	}
+	const patchedDependencies = config?.patchedDependencies;
+	if (patchedDependencies == null) return {};
+	if (typeof patchedDependencies !== "object" || Array.isArray(patchedDependencies)) throw new MastraError({
+		id: "DEPLOYER_INVALID_PNPM_PATCH_CONFIG",
+		domain: ErrorDomain.DEPLOYER,
+		category: ErrorCategory.USER,
+		text: "pnpm patchedDependencies must be a dependency-to-patch-path mapping"
+	});
+	for (const [dependency, patchPath] of Object.entries(patchedDependencies)) if (!dependency.trim() || typeof patchPath !== "string" || !patchPath.trim()) throw new MastraError({
+		id: "DEPLOYER_INVALID_PNPM_PATCH_CONFIG",
+		domain: ErrorDomain.DEPLOYER,
+		category: ErrorCategory.USER,
+		details: { dependency },
+		text: `Invalid pnpm patch path for ${dependency || "<empty dependency>"}`
+	});
+	return patchedDependencies;
+}
+async function copyPnpmPatchFiles(source, sourceWorkspaceYamlPath, outputDir) {
+	const configuredPatches = readPnpmPatchedDependencies(source);
+	const entries = Object.entries(configuredPatches);
+	if (entries.length === 0) return {};
+	const sourceRoot = dirname(sourceWorkspaceYamlPath);
+	const outputPatchDir = path.join(outputDir, ".mastra-patches");
+	await fsPromises.mkdir(outputPatchDir, { recursive: true });
+	const copiedPatches = {};
+	for (const [index, [dependency, configuredPath]] of entries.entries()) {
+		const sourcePatchPath = path.isAbsolute(configuredPath) ? configuredPath : path.resolve(sourceRoot, configuredPath);
+		const safeBasename = path.basename(configuredPath).replace(/[^a-zA-Z0-9._-]/g, "_") || "dependency.patch";
+		const outputBasename = `${index}-${safeBasename}`;
+		await fsPromises.copyFile(sourcePatchPath, path.join(outputPatchDir, outputBasename));
+		copiedPatches[dependency] = `.mastra-patches/${outputBasename}`;
+	}
+	return copiedPatches;
+}
 var Deps = class extends MastraBase {
 	packageManager;
 	rootDir;
@@ -232,7 +283,11 @@ var Deps = class extends MastraBase {
 	async writePnpmConfig(dir, options = {}) {
 		const sourceWorkspaceYamlPath = this.findPnpmWorkspaceFile(this.rootDir);
 		const sourceWorkspaceYaml = sourceWorkspaceYamlPath ? await fsPromises.readFile(sourceWorkspaceYamlPath, "utf-8") : "";
-		await fsPromises.writeFile(path.join(dir, "pnpm-workspace.yaml"), copyPnpmWorkspaceSettings(sourceWorkspaceYaml, options), "utf-8");
+		const patchedDependencies = sourceWorkspaceYamlPath ? await copyPnpmPatchFiles(sourceWorkspaceYaml, sourceWorkspaceYamlPath, dir) : {};
+		await fsPromises.writeFile(path.join(dir, "pnpm-workspace.yaml"), copyPnpmWorkspaceSettings(sourceWorkspaceYaml, {
+			...options,
+			patchedDependencies
+		}), "utf-8");
 	}
 	async writeYarnConfig(dir, options) {
 		const yarnrcPath = path.join(dir, ".yarnrc.yml");
diff --git a/dist/services-BLvNG7v7.cjs b/dist/services-BLvNG7v7.cjs
index 87c6bced303692232d03c5d2e1114b849f166534..0c9fbc78084fb618a4f2c2343800783fff4a2ebe 100644
--- a/dist/services-BLvNG7v7.cjs
+++ b/dist/services-BLvNG7v7.cjs
@@ -171,9 +171,60 @@ function copyPnpmWorkspaceSettings(source, options = {}) {
 		blocks.push(architectureBlock.join("\n"));
 	}
 	if (options.pnpmOverrides && Object.keys(options.pnpmOverrides).length > 0) blocks.push(["overrides:", ...Object.entries(options.pnpmOverrides).map(([key, value]) => `  ${JSON.stringify(key)}: ${JSON.stringify(value)}`)].join("\n"));
+	if (options.patchedDependencies && Object.keys(options.patchedDependencies).length > 0) {
+		blocks.push(["patchedDependencies:", ...Object.entries(options.patchedDependencies).map(([key, value]) => `  ${JSON.stringify(key)}: ${JSON.stringify(value)}`)].join("\n"));
+		blocks.push("allowUnusedPatches: true");
+	}
 	if (options.pnpmNodeLinker) blocks.push(`nodeLinker: ${options.pnpmNodeLinker}`);
 	return ["packages:\n  - '.'", ...blocks].join("\n\n") + "\n";
 }
+function readPnpmPatchedDependencies(source) {
+	if (!source.trim()) return {};
+	let config;
+	try {
+		config = (0, yaml.parse)(source);
+	} catch (error) {
+		throw new _mastra_core_error.MastraError({
+			id: "DEPLOYER_INVALID_PNPM_PATCH_CONFIG",
+			domain: _mastra_core_error.ErrorDomain.DEPLOYER,
+			category: _mastra_core_error.ErrorCategory.USER,
+			text: "Invalid pnpm patchedDependencies configuration"
+		}, error);
+	}
+	const patchedDependencies = config?.patchedDependencies;
+	if (patchedDependencies == null) return {};
+	if (typeof patchedDependencies !== "object" || Array.isArray(patchedDependencies)) throw new _mastra_core_error.MastraError({
+		id: "DEPLOYER_INVALID_PNPM_PATCH_CONFIG",
+		domain: _mastra_core_error.ErrorDomain.DEPLOYER,
+		category: _mastra_core_error.ErrorCategory.USER,
+		text: "pnpm patchedDependencies must be a dependency-to-patch-path mapping"
+	});
+	for (const [dependency, patchPath] of Object.entries(patchedDependencies)) if (!dependency.trim() || typeof patchPath !== "string" || !patchPath.trim()) throw new _mastra_core_error.MastraError({
+		id: "DEPLOYER_INVALID_PNPM_PATCH_CONFIG",
+		domain: _mastra_core_error.ErrorDomain.DEPLOYER,
+		category: _mastra_core_error.ErrorCategory.USER,
+		details: { dependency },
+		text: `Invalid pnpm patch path for ${dependency || "<empty dependency>"}`
+	});
+	return patchedDependencies;
+}
+async function copyPnpmPatchFiles(source, sourceWorkspaceYamlPath, outputDir) {
+	const configuredPatches = readPnpmPatchedDependencies(source);
+	const entries = Object.entries(configuredPatches);
+	if (entries.length === 0) return {};
+	const sourceRoot = path.default.dirname(sourceWorkspaceYamlPath);
+	const outputPatchDir = path.default.join(outputDir, ".mastra-patches");
+	await fs_promises.default.mkdir(outputPatchDir, { recursive: true });
+	const copiedPatches = {};
+	for (const [index, [dependency, configuredPath]] of entries.entries()) {
+		const sourcePatchPath = path.default.isAbsolute(configuredPath) ? configuredPath : path.default.resolve(sourceRoot, configuredPath);
+		const safeBasename = path.default.basename(configuredPath).replace(/[^a-zA-Z0-9._-]/g, "_") || "dependency.patch";
+		const outputBasename = `${index}-${safeBasename}`;
+		await fs_promises.default.copyFile(sourcePatchPath, path.default.join(outputPatchDir, outputBasename));
+		copiedPatches[dependency] = `.mastra-patches/${outputBasename}`;
+	}
+	return copiedPatches;
+}
 var Deps = class extends _mastra_core_base.MastraBase {
 	packageManager;
 	rootDir;
@@ -236,7 +287,11 @@ var Deps = class extends _mastra_core_base.MastraBase {
 	async writePnpmConfig(dir, options = {}) {
 		const sourceWorkspaceYamlPath = this.findPnpmWorkspaceFile(this.rootDir);
 		const sourceWorkspaceYaml = sourceWorkspaceYamlPath ? await fs_promises.default.readFile(sourceWorkspaceYamlPath, "utf-8") : "";
-		await fs_promises.default.writeFile(path.default.join(dir, "pnpm-workspace.yaml"), copyPnpmWorkspaceSettings(sourceWorkspaceYaml, options), "utf-8");
+		const patchedDependencies = sourceWorkspaceYamlPath ? await copyPnpmPatchFiles(sourceWorkspaceYaml, sourceWorkspaceYamlPath, dir) : {};
+		await fs_promises.default.writeFile(path.default.join(dir, "pnpm-workspace.yaml"), copyPnpmWorkspaceSettings(sourceWorkspaceYaml, {
+			...options,
+			patchedDependencies
+		}), "utf-8");
 	}
 	async writeYarnConfig(dir, options) {
 		const yarnrcPath = path.default.join(dir, ".yarnrc.yml");
