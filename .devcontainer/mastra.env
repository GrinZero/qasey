# Keep Mastra's dev child process in development mode without loading the
# host-oriented .env.local file inside the container. Runtime credentials and
# service endpoints continue to come from the Compose environment.
NODE_ENV=development
