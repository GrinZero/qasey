# Keep Mastra's generated child process in development mode. Qasey's runtime
# loader finds the mounted project root and merges its standard env files;
# values already supplied by Compose retain precedence.
NODE_ENV=development
